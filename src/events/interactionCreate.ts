import { ChatInputCommandInteraction, EventRun } from "discord.js";

export const name = "interactionCreate"; // This is the event name.

export const run: EventRun = async (bot, interaction: ChatInputCommandInteraction) => {

};